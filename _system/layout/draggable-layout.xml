<xten name="draggable-layout" element="div">
    <mandatory>name</mandatory>
    <style>
        position: relative;
    </style>

    <initElement>
    <%
        /* to do - need coding 
        const scaleChildDivs = () => {
            const parentWidth = element.offsetWidth;
            const originalParentWidth = 100;
            const scaleRatio = parentWidth / originalParentWidth;

            const childDivs = element.querySelectorAll('.wx-xten-draggable');

            childDivs.forEach(childDiv => {
                const originalWidth = parseFloat(childDiv.getAttribute('data-original-width'));
                const originalLeft = parseFloat(childDiv.getAttribute('data-original-left'));

                const scaledWidth = originalWidth * scaleRatio;
                const scaledLeft = originalLeft * scaleRatio;

                childDiv.style.width = `${scaledWidth}px`;
                childDiv.style.left = `${scaledLeft}px`;
            });
        };

        element.addEventListener('resize', scaleChildDivs);
        */
    %>
    </initElement>

</xten>
