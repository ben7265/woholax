<xten name="list" extend="_list" element="div">
    <allow>owner, admin</allow>
    <cssNode>
        #[$id] > .wx-view-datalist {
            display: flex;
            flex-direction: [$orientation||column];
            gap: [$item-gap||0];
            width: 100%;
        }

        #[$id][orientation="row"] > .wx-view-datalist > .wx-xten-listitem {
            flex-grow: [$grow||1];
            flex-shrink: [$shrink||0];
            flex-basis: 0px;
        }
    </cssNode>
</xten>
