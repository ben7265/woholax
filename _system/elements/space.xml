<xten name="space" element="_children">
    <init>
        <%
        const n = parseInt(node.getValue(node.attribs.n) || 1);
        node.text = '';
        for (var i = 0; i < n; i++) {
            node.text += '&nbsp;';
        }
        %>
    </init>
</xten>
