<xten name="column" element="div">
    <style>
        position: relative;
    </style>
    <nodeStyle>
        flex: [$ratio||1] [$ratio||0] 1px;
    </nodeStyle>
    <!--init>
        const width = node.getValue('width');
        if (width) {
            node.addStyle('flex: 1 0 ' + width);
            return;
        }

        const ratio = node.getValue('ratio');
        if (ratio) {
            node.addStyle('flex: ' + ratio + ' ' + ratio + ' 1px');
            return;
        }

        const flex = node.getValue('flex');
        if (flex) {
            node.addStyle('flex: ' + flex);
            return;
        }

        node.addStyle('flex: 1 0 1px');
    </init-->
</xten>
